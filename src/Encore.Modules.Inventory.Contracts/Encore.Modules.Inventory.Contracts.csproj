<Project Sdk="Microsoft.NET.Sdk">

  <!-- The only project another module may reference (017), so it depends on nothing. -->
  <PropertyGroup>
    <EncoreZeroDependency>true</EncoreZeroDependency>
  </PropertyGroup>

</Project>
