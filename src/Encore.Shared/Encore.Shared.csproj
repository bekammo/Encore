<Project Sdk="Microsoft.NET.Sdk">

  <!-- The Domain's only project reference, so it takes the Domain's zero-dependency rules (002). -->
  <PropertyGroup>
    <EncoreZeroDependency>true</EncoreZeroDependency>
  </PropertyGroup>

</Project>
