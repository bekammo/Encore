<Project Sdk="Microsoft.NET.Sdk">

  <!-- Catalog's public face: the only thing another module may know about it.
       No packages and no project references (ENCORE001-003 in Directory.Build.targets). -->
  <PropertyGroup>
    <EncoreZeroDependency>true</EncoreZeroDependency>
  </PropertyGroup>

</Project>
